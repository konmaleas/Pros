QUICK START - Apply Changes in 5 Minutes
==========================================

STEP 1: Open pattern_analyzer.py
Find line ~292 (in _score_anchor_candidate method):
    if viable_files < len(all_file_data):
Change to:
    if viable_files == 0:

This is THE critical fix that enables batch processing!

STEP 2: Update analyze_all_files return type (line ~117):
Change:
    def analyze_all_files(...) -> Dict[str, FileAnalysisResult]:
To:
    def analyze_all_files(...) -> Tuple[Dict[str, FileAnalysisResult], Dict]:

STEP 3: Add processed_lines to all_file_data (line ~142):
Change:
    all_file_data[str(file_path)] = {'filtered_lines': ..., 'chunk_positions': ...}
To:
    all_file_data[str(file_path)] = {'processed_lines': processed_lines,
                                     'filtered_lines': ..., 
                                     'chunk_positions': ...}

STEP 4: Update 3 return statements in analyze_all_files:
All "return ..." should become "return ..., all_file_data"

STEP 5: Add convert_validation_to_file_results() method at end of class
(Copy from COMPLETE_CHANGES_GUIDE.md)

STEP 6: Open batch_processor.py
Add found_titles field to CaseResult (line ~30):
    found_titles: Dict[str, str] = field(default_factory=dict)

STEP 7: Update process_case() (line ~158):
Change:
    results = analyzer.analyze_all_files(extract_and_process)
To:
    results, all_file_data = analyzer.analyze_all_files(extract_and_process)

Then add multi-anchor fallback logic (copy from guide)

STEP 8: Add found_titles to report generation (copy from guide)

DONE! Test with: python batch_processor.py

Full details in: COMPLETE_CHANGES_GUIDE.md
